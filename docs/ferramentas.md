# Ferramentas

Bybit expõe 1 ferramenta (todas somente leitura).

### 1. `search_tools`
**Input**: `action` (opcional), `query` (opcional), `min_score` (opcional), `force_reindex` (opcional), `queries` (opcional), `tool_id` (opcional), `arguments` (opcional)

Single entrypoint for MCP catalog.

## Prompts de exemplo

```
Qual meu saldo total na Bybit?
Quais posições eu tenho abertas e qual o resultado não realizado?
Quanto paguei de taxa nos meus trades da última semana?
```
